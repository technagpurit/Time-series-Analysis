# -*- coding: utf-8 -*-
"""Untitled24.ipynb

Automatically generated by Colaboratory.

Original file is located at
    https://colab.research.google.com/drive/1JggIkAknS-upHbTHmWSKF9cDO_V-TNvv
"""

!pip install PyEMD
!pip install EMD-signal
from PyEMD import EMD

import PyEMD;
print(PyEMD.__version__)

import numpy as np

from pandas import read_csv

TS=read_csv('newdataset.csv')

data = TS.values

emd = EMD()

from pandas import read_csv

TS=read_csv('nd.csv')

data=TS.values

d1=np.reshape(data,(200,))

d1.shape

import pylab as plt

from PyEMD import EMD, Visualisation

emd.emd(d1)

imfs, res = emd.get_imfs_and_residue()

vis = Visualisation()

vis.plot_imfs(imfs=imfs, residue=res, t=t, include_residue=True)
vis.plot_instant_freq(t, imfs=imfs)
vis.show()



from PyEMD import EEMD

if __name__ == "__main__":
  eemd = EEMD()
  eIMFs = eemd(d1)

eimfs, res = eemd.get_imfs_and_residue()

vis.plot_imfs(imfs=eimfs, residue=res, t=t, include_residue=True)
vis.plot_instant_freq(t, imfs=imfs)
vis.show()

from PyEMD import CEEMDAN

ceemdan = CEEMDAN()

cIMFs = ceemdan(d1)

vis.plot_imfs(imfs=cIMFs, residue=res, t=t, include_residue=True)
vis.plot_instant_freq(t, imfs=imfs)
vis.show()

t = np.arange(0, 3, 0.01)

